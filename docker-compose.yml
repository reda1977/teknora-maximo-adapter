services:
  migrator:
    build: .
    container_name: maximo_migrator
    ports:
      # اتأكدنا فعليًا بـ "ss -tlnp" إن 8800 فاضي على السيرفر (اللي كان
      # متاخد هو 8088 - بورت Superset الافتراضي، مش 8800)
      - "8800:8800"
    restart: unless-stopped
